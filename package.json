{
  "scripts": {
    "dev": "concurrently \"spago build --watch\" \"parcel index.html\"",
    "build": "spago build",
    "start": "parcel index.html"
  },
  "dependencies": {
    "purescript": "^0.15.7",
    "spago": "^0.20.9"
  },
  "devDependencies": {
    "concurrently": "^7.5.0",
    "parcel-bundler": "^1.12.5"
  }
}
